language: cpp
os: linux

jobs:
  include:
    - dist: trusty
    - dist: xenial
    - dist: trusty
    - dist: precise

compiler:
  - clang
  - gcc

script:
  - mkdir build && cd build && cmake ..
  - make
  - ctest -V
